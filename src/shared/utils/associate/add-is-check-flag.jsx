export const addIsCheckFlag = (rows, selected) => {  return (    rows &&    rows.map((row, index) => {      row.isChecked = false;      selected.forEach((selectedIndex) => {        if (selectedIndex === index) {          row.isChecked = true;        }      });      return row;    }, selected)  );};