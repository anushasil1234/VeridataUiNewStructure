import React, { useEffect, useState } from 'react';import { apiService } from './api-functions';import { useDispatch } from 'react-redux';import { storeApi } from 'store/slices/api-slice';import Modals from 'shared/utils/app-wrapper/modal';const initialState = {  confirmation: false,  consent: false,  info: false,  otpForm: false,  otpSubmission: false,  remarks: false,  document: false,  submit: false,  remedy: false,  profilePassword: false,  filePassword: false,};const AppWrapper = (App) => {  const openModal = (modal) => {    setModalState((prevState) => ({ ...prevState, [modal]: true }));  };  const closeModal = (modal) => {    setModalState((prevState) => ({ ...prevState, [modal]: false }));  };  const dispatch = useDispatch();  useEffect(() => {    dispatch(storeApi(apiService));   }, [dispatch]);  return (    <>      <App />      <Modals modalState={modalState} closeModal={closeModal} />    </>  );};export default AppWrapper;