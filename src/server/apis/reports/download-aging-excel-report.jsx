import ServerRequest from 'server/utils/server-request';import downloadFile from 'shared/utils/associate/download-file';import showErrorMessage from 'shared/utils/associate/show-error-message';const downloadAgingExcelReport = async (_url, payLoad) => {  try {    const response = await ServerRequest(_url, 'POST', payLoad, null, true);    if (response && response.responseInfo) {      const { fileData, fileName } = response.responseInfo;       if (fileData && fileName) {        const linkSource = `data:application/vnd.openxmlformats-officedocument.spreadsheetml.sheet;base64,${fileData}`;        downloadFile(linkSource, fileName);      } else {        showErrorMessage('Missing fileData or fileName');      }    } else {      console.error('Invalid response from server');    }  } catch (error) {    showErrorMessage(error);  }};export { downloadAgingExcelReport };